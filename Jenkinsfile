pipeline {
    agent any

    stages {
        
        stage('Build') {
            steps {
                sh 'echo "Pipeline executed successfully!"'
                sh 'echo "Added an branch"'
            }
        }
    }
}
